{
  "name": "scott-reader",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute scott-reader --file=migrations/0001_initial.sql",
    "db:init:remote": "wrangler d1 execute scott-reader --remote --file=migrations/0001_initial.sql",
    "test": "vitest run"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.2",
    "wrangler": "^4.80.0"
  }
}
